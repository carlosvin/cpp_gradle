apply plugin: 'cpp'

model {
    buildTypes {
        debug
        release
    }
}

libraries {
    hello {}
}


executables {
    main {
        binaries.all {
            lib libraries.hello.shared
        }
    }
}